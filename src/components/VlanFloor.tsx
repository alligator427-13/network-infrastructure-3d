// VLAN Floor Component

import { useMemo } from 'react';
import type { NetworkNode } from '../types';

interface VlanFloorProps {
  nodes: NetworkNode[];
  theme: any;
}

const VlanFloor: React.FC<VlanFloorProps> = ({ nodes }) => {
  return null;
};

export default VlanFloor;
